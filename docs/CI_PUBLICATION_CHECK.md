# CI Publication Check

This branch exists to execute the repository's pull-request verification workflow against the fully published source tree before the documentation-only changes are merged into `main`.
